#!/usr/bin/env raku

use lib '/usr/local/share/aether';

use Aether;

sub MAIN(
    Str $file,
    Bool :$strict = False
) {

    my $source = slurp $file;

    my $output =
        run-aether(
            $source,
            :$strict
        );

    say $output
        if $output.chars > 0;
}